/*
import { useState } from 'react';
import { getProductosFront } from '../fetchers/tienda';


export const ChangeMessengerMessage = ({ currentMessage }: { currentMessage: memory() }) => {
  const [message, setMessage] = useState(currentMessage);

  return (
    <div
      style={{
        display: 'flex',
        flexDirection: 'column',
        width: '500px',
        margin: 'auto',
        gap: '10px'
      }}
    >
      <input
        placeholder="Change Message TEXT"
        value={message}
        onChange={(e) => setMessage(e.currentTarget.value)}
      />
      <button onClick={async () => await setMessengerMessage(message)}>Change Message TEXT</button>
    </div>
  );
};
*/